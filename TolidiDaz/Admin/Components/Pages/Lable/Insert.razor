@using Dto.Models.DtoLable
@inject SweetAlertService Swal
@inject ILocalStorageService localStorageService
@inject NavigationManager NavManager
<EditForm Model="addJsonLable" OnValidSubmit="Add" FormName="AddForm">
    <DataAnnotationsValidator />

    <div class="bg-white py-6 dark:bg-gray-800 rounded-xl shadow-md dark:shadow-gray-700 overflow-hidden border border-gray-200 dark:border-gray-700">
        <div class="px-6">
            <div class="flex items-end gap-3">

                <div class="flex-1">
                    <CustomLabel ForId="Blog-Title" For="@(() => addJsonLable.Text)" />
                    <ValidationMessage For="@(() => addJsonLable.Text)" class="validate" />

                    <InputText id="Blog-Title"
                               @bind-Value="addJsonLable.Text"
                               class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-primary focus:border-primary dark:bg-gray-700 dark:border-gray-600 dark:text-white" />
                </div>

                <div class="mb-1">
                    <button type="submit" class="px-4 py-2 text-sm font-medium rounded-lg bg-primary-600 text-white hover:bg-primary-500 shadow-sm dark:bg-primary-500 dark:hover:bg-primary-600">
                        ذخیره
                    </button>
                </div>

            </div>
        </div>
    </div>
</EditForm>



@* <EditForm Model="addJsonLable" OnValidSubmit="Add" FormName="AddForm">
    <DataAnnotationsValidator />
    <div class="md-12" style="display:flex;align-content:space-between;">
        <div class="form-group col-10" >
            <ValidationMessage For="@(() => addJsonLable.Text)" class="validate" />
            <InputText @bind-Value="addJsonLable.Text" class="form-control" />
        </div>


        <div class="form-group col-2">
            <button class="btn btn-primary" style="margin-right:5px;" @onclick="@(async () => await Add())">ثبت</button>
        </div>
    </div>
    
</EditForm> *@
@code {

    public AddJsonLable? addJsonLable { get; set; } = new();
    [Parameter]
    public EventCallback<AddJsonLable> Save { get; set; }

    private async Task Add()
    {
        addJsonLable.ID = Guid.NewGuid();
        await Save.InvokeAsync(addJsonLable);
        addJsonLable = new AddJsonLable();
       
    }
   
}