@page "/SliderFiles/{guid}"
@using Admin.Services.Slider
@inject NavigationManager NavManager
<AccessAdminRoles />
<ManageImagesPublicByFtp TService="SliderService"
                         Guid="@guid"
                         BackUrl="/Slider" />
@code {
    [Parameter]
    public string? guid { get; set; }
}
