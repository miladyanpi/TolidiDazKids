@page "/StoryFiles/{guid}"
@using Admin.Services.Story

<AccessAdminRoles />
<ManageImagesPublicByFtp TService="StoryService"
                         Guid="@guid"
                         BackUrl="/Story" />
@code {
    [Parameter]
    public string? guid { get; set; }
}
