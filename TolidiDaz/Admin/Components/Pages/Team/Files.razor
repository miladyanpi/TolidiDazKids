@page "/TeamFiles/{guid}"
@using Admin.Services.Team

<AccessAdminRoles />
<ManageImagesPublicByFtp TService="TeamService"
                         Guid="@guid"
                         BackUrl="/Team" />
@code {
    [Parameter]
    public string? guid { get; set; }
}
