@page "/Login"
@using CurrieTechnologies.Razor.SweetAlert2
@using Dto.Models.Constant
@using Dto.Models.DtoAccount
@using Dto.Models.DtoSmsModel
@using Microsoft.AspNetCore.Components.Forms
@using Dto.Models.ResponseApi
@using Microsoft.AspNetCore.Components.Authorization
@using Microsoft.JSInterop
@using PublicTolidiAyhan.Services
@using PublicTolidiAyhan.Components.Layout
@using PublicTolidiAyhan.Constant
@using RestSharp
@using PublicTolidiAyhan.Components.Pages.DisplayNameLabel
@inject SweetAlertService Swal
@inject NavigationManager NavManager
@inject IRootApi<ResponseApiEntity<ResultLoginAccount>> RootApiLogin
@inject IRootApi<ResponseApiEntity<ResultLoginAccount>> RootApiSendSmsOtpCode
@inject AuthenticationStateProvider AuthStateProvider
@inject IJSRuntime JS
@layout LoginLayout

<div class="min-h-screen flex flex-col lg:flex-row">
    <div class="lg:w-1/2 auth-bg relative hidden lg:flex items-center jusstify-center p-12">
        <div class="absolute inset-0 bg-black/30 dark:bg-black/50"></div>
        <div class="relative z-10 text-white text-center max-w-md">
            <div class="w-20 h-20 rounded-xl bg-green-600 flex items-center justify-center mx-auto mb-6">
                <a href="/">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-10 w-10 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
                    </svg>
               </a>
                
            </div>
            <h2 class="text-3xl font-bold mb-4">به سایت ما خوش آمدید</h2>
            <p class="text-lg mb-6">با ثبت نام در @_PublicSettingService.ResultPublicSetting.Name کالا از بهترین تجربه خرید آنلاین لذت ببرید. دسترسی به هزاران محصول با کیفیت و قیمت مناسب.</p>
        </div>
    </div>

    <div class="lg:w-1/2 flex items-center justify-center p-6">
        <div class="w-full max-w-md">
            <div class="flex items-center justify-center mb-8 lg:hidden">
                <div class="w-12 h-12 rounded-md bg-green-600 flex items-center justify-center">
                    <a  href="/">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
                        </svg>
                    </a>
                   
                </div>
                <a href="/">
                    <h1 class="mr-3 text-xl font-bold text-dark dark:text-light">فروشگاه<span class="text-primary"> @_PublicSettingService.ResultPublicSetting.Name </span></h1>
                </a>
            </div>

            <div class="bg-white dark:bg-gray-800 rounded-2xl shadow-soft dark:shadow-soft-dark p-8 border border-gray-100 dark:border-gray-700 fade-in">
                <div class="text-center mb-8">
                    <h2 class="text-2xl font-bold text-dark dark:text-light mb-2">ورود به حساب کاربری</h2>
                    <p class="text-gray-500 dark:text-gray-400">روش ورود را انتخاب کنید</p>
                </div>

              @*   <div class="flex mb-6 rounded-xl overflow-hidden border border-gray-200 dark:border-gray-600">
                    <button id="password-tab" class="tab-button flex-1 py-3 px-4 text-sm font-medium active" onclick="switchTab('password')">
                        ورود با رمز عبور
                    </button>
                    <button id="sms-tab" class="tab-button flex-1 py-3 px-4 text-sm font-medium" onclick="switchTab('sms')">
                        ورود با پیامک
                    </button>
                </div> *@
                <div class="flex mb-6 rounded-xl overflow-hidden border border-gray-200 dark:border-gray-600">
                  
                    <button class="@(isPasswordTabActive ? "tab-button flex-1 py-3 px-4 text-sm font-medium active" : "tab-button flex-1 py-3 px-4 text-sm font-medium")" @onclick="@(() => SwitchToPasswordTab())">ورود با رمز عبور</button>
                    <button class="@(isPasswordTabActive ? "tab-button flex-1 py-3 px-4 text-sm font-medium" : "tab-button flex-1 py-3 px-4 text-sm font-medium active")" @onclick="@(() => SwitchToSmsTab())">ورود با پیامک</button>
                </div>
                <EditForm Model="loginAccount" FormName="LoginForm" OnValidSubmit="LoginAccount"  class="@(isPasswordTabActive ? "space-y-5 active" : "space-y-5 hidden")">
                    <DataAnnotationsValidator />
                    <div>
                        <CustomLabel For="@(() => loginAccount.UserName)"></CustomLabel>
                        <ValidationMessage For="@(() => loginAccount.UserName)" class="validate" />
                        <div class="relative">
                            <InputText @bind-Value="loginAccount.UserName" placeholder="نام کاربری یا شماره موبایل" class="w-full pl-12 pr-4 py-3 border rounded-xl focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent dark:bg-gray-700 dark:border-gray-600 dark:text-white " style="text-align:left" />

                            <div class="absolute input-icon top-1/2 transform -translate-y-1/2 text-gray-400">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                                </svg>
                            </div>
                        </div>
                        <p class="error-message hidden" >لطفا نام کاربری یا شماره موبایل معتبر وارد کنید</p>
                    </div>
                    <div>
                        <CustomLabel For="@(() => loginAccount.Password)"></CustomLabel>
                        <ValidationMessage For="@(() => loginAccount.Password)" class="validate"  />
                        <div class="relative">
                            <InputText  @bind-Value="loginAccount.Password" style="text-align:left" type="password" class="w-full pl-12 pr-4 py-3 border rounded-xl focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent dark:bg-gray-700 dark:border-gray-600 dark:text-white" />
                            <div class="absolute input-icon top-1/2 transform -translate-y-1/2 text-gray-400">
                            </div>
                            <button type="button" class="absolute password-toggle top-1/2 transform -translate-y-1/2 text-gray-400" >
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
                                </svg>
                            </button>
                        </div>
                        <p class="error-message hidden">رمز عبور باید حداقل ۶ کاراکتر باشد</p>
                    </div>

                    <div class="flex items-center justify-between">
                        <div class="flex items-center">
                            <input name="remember-me" type="checkbox" class="h-4 w-4 text-primary dark:text-primary-dark focus:ring-primary dark:focus:ring-primary-dark border-gray-300 dark:border-gray-600 rounded">
                            <label for="remember-me" class="mr-2 block text-sm text-gray-700 dark:text-gray-300">مرا به خاطر بسپار</label>
                        </div>
                      @*   <div class="text-sm">
                            <a href="#" @onclick="SwitchToSmsTab" class="font-medium text-primary dark:text-primary-dark hover:underline">رمز عبور را فراموش کرده‌اید؟</a>
                        </div> *@
                    </div>
                    <div class="flex items-center justify-between">
                        <div class="text-sm">
                            <span class="font-medium text-primary dark:text-primary-dark ">در صورت فراموشی رمز از طریق ورود با پیامک اقدام نمایید</span>
                        </div>
                    </div>
                    <div>
                        <button type="submit" class="w-full flex justify-center items-center py-3 px-4 border border-transparent rounded-xl shadow-sm text-sm font-medium text-white bg-green-600 hover:bg-green-500 dark:hover:bg-green-500 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary transition-colors">
                            ورود به حساب
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
                                <path fill-rule="evenodd" d="M3 3a1 1 0 011 1v12a1 1 0 11-2 0V4a1 1 0 011-1zm7.707 3.293a1 1 0 010 1.414L9.414 9H17a1 1 0 110 2H9.414l1.293 1.293a1 1 0 01-1.414 1.414l-3-3a1 1 0 010-1.414l3-3a1 1 0 011.414 0z" clip-rule="evenodd" />
                            </svg>
                        </button>
                    </div>
              
                </EditForm>
                <EditForm Model="SendSmsOtpCode" FormName="LoginSmsForm" OnValidSubmit="LoginBySmsCode"  class="@(isPasswordTabActive ? "space-y-5 hidden" : "space-y-5 active")">
                    <DataAnnotationsValidator />
                    <div>
                        <label for="mobile" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">شماره موبایل</label>
                        <div class="relative">
                            <ValidationMessage For="@(() => SendSmsOtpCode.PhoneNumber)" class="validate" />
                            <InputText   @bind-Value="@SendSmsOtpCode.PhoneNumber"  type="tel"  class="w-full pl-12 pr-4 py-3 border rounded-xl focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent dark:bg-gray-700 dark:border-gray-600 dark:text-white" placeholder="09xxxxxxxxx" />
                            <div class="absolute input-icon top-1/2 transform -translate-y-1/2 text-gray-400">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
                                </svg>
                            </div>
                        </div>
                        <p class="error-message hidden" >لطفا شماره موبایل معتبر وارد کنید</p>
                    </div>

                    <div  class="@(CodeSended==true?"":"hidden")">
                        <label for="otp-code" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">کد تایید</label>
                        <div class="relative">
                            <ValidationMessage For="@(() => verifySendSmsOtpCode.Code)" class="validate" />
                            <InputText @bind-Value="@verifySendSmsOtpCode.Code" type="text"  class="w-full pl-12 pr-4 py-3 border rounded-xl focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent dark:bg-gray-700 dark:border-gray-600 dark:text-white" placeholder="کد 6 رقمی ارسال شده"  />
                            <div class="absolute input-icon top-1/2 transform -translate-y-1/2 text-gray-400">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
                                </svg>
                            </div>
                        </div>
                        <div class=" @(CodeSended == true ? "flex items-center justify-between mt-2" : "flex items-center justify-between mt-2 hidden")">
                            <p class="error-message hidden" >کد تایید نامعتبر است</p>
                            <div id="countdown" class="text-sm text-gray-500 dark:text-gray-400 hidden">
                                ارسال مجدد کد پس از <span id="countdown-time">120</span> ثانیه
                            </div>
                            <button type="submit" id="resend-otp" class="text-sm font-medium text-primary dark:text-primary-dark hover:underline hidden">
                                ارسال مجدد کد
                            </button>
                        </div>
                    </div>

                    <div>
                        <button type="submit" class=" @(CodeSended == true == true ? "w-full flex justify-center items-center py-3 px-4 border border-transparent rounded-xl shadow-sm text-sm font-medium text-white bg-green-600 dark:bg-green-600 hover:bg-green-500 dark:hover:bg-green-500 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary transition-colors hidden" : "w-full flex justify-center items-center py-3 px-4 border border-transparent rounded-xl shadow-sm text-sm font-medium text-white bg-green-600 dark:bg-green-600 hover:bg-green-500 dark:hover:bg-green-500 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary transition-colors")">
                            دریافت کد تایید
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-5l-5 5v-5z" />
                            </svg>
                        </button>
                        <button @onclick="@(async () => await VerifySmsCode())" type="button" class=" @(CodeSended == true ? "w-full flex justify-center items-center py-3 px-4 border border-transparent rounded-xl shadow-sm text-sm font-medium text-white bg-green-600 dark:bg-green-600 hover:bg-green-500 dark:hover:bg-green-500 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary transition-colors" : "w-full flex justify-center items-center py-3 px-4 border border-transparent rounded-xl shadow-sm text-sm font-medium text-white bg-green-600 dark:bg-green-600 hover:bg-green-500 dark:hover:bg-green-500 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary transition-colors hidden")">
                            تایید و ورود
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
                                <path fill-rule="evenodd" d="M3 3a1 1 0 011 1v12a1 1 0 11-2 0V4a1 1 0 011-1zm7.707 3.293a1 1 0 010 1.414L9.414 9H17a1 1 0 110 2H9.414l1.293 1.293a1 1 0 01-1.414 1.414l-3-3a1 1 0 010-1.414l3-3a1 1 0 011.414 0z" clip-rule="evenodd" />
                            </svg>
                        </button>
                    </div>
                  </EditForm>
                <div class="mt-6">
                    <div class="relative">
                        <div class="absolute inset-0 flex items-center">
                            <div class="w-full border-t border-gray-300 dark:border-gray-600"></div>
                        </div>
                        <div class="relative flex justify-center text-sm">
                            <span class="px-2 bg-white dark:bg-gray-800 text-gray-500 dark:text-gray-400">یا ورود با</span>
                        </div>
                    </div>

                    <div class="mt-6 grid grid-cols-3 gap-3">
                        <div>
                            <a href="#" class="w-full inline-flex justify-center items-center py-2 px-4 border border-gray-300 dark:border-gray-600 rounded-xl shadow-sm bg-white dark:bg-gray-700 text-sm font-medium text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-600">
                                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                                    <path fill-rule="evenodd" d="M10 0C4.477 0 0 4.477 0 10c0 4.42 2.865 8.166 6.839 9.489.5.092.682-.217.682-.482 0-.237-.008-.866-.013-1.7-2.782.603-3.369-1.342-3.369-1.342-.454-1.155-1.11-1.462-1.11-1.462-.908-.62.069-.608.069-.608 1.003.07 1.531 1.03 1.531 1.03.892 1.529 2.341 1.087 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.11-4.555-4.943 0-1.091.39-1.984 1.029-2.683-.103-.253-.446-1.27.098-2.647 0 0 .84-.269 2.75 1.025A9.564 9.564 0 0110 4.844c.85.004 1.705.114 2.504.336 1.909-1.294 2.747-1.025 2.747-1.025.546 1.377.203 2.394.1 2.647.64.699 1.028 1.592 1.028 2.683 0 3.842-2.339 4.687-4.566 4.933.359.309.678.919.678 1.852 0 1.336-.012 2.415-.012 2.743 0 .267.18.578.688.48C17.14 18.163 20 14.418 20 10c0-5.523-4.477-10-10-10z" clip-rule="evenodd" />
                                </svg>
                            </a>
                        </div>

                        <div>
                            <a href="#" class="w-full inline-flex justify-center items-center py-2 px-4 border border-gray-300 dark:border-gray-600 rounded-xl shadow-sm bg-white dark:bg-gray-700 text-sm font-medium text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-600">
                                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                                    <path fill-rule="evenodd" d="M10 0C4.477 0 0 4.477 0 10c0 5.523 4.477 10 10 10 5.523 0 10-4.477 10-10C20 4.477 15.523 0 10 0zm6.25 14.5c-.41.41-1.08.41-1.5 0l-3.75-3.75-3.75 3.75c-.41.41-1.08.41-1.5 0-.41-.42-.41-1.08 0-1.5l3.75-3.75-3.75-3.75c-.41-.42-.41-1.08 0-1.5.42-.41 1.08-.41 1.5 0l3.75 3.75 3.75-3.75c.42-.41 1.08-.41 1.5 0 .41.42.41 1.08 0 1.5l-3.75 3.75 3.75 3.75c.41.42.41 1.08 0 1.5z" clip-rule="evenodd" />
                                </svg>
                            </a>
                        </div>

                        <div>
                            <a href="#" class="w-full inline-flex justify-center items-center py-2 px-4 border border-gray-300 dark:border-gray-600 rounded-xl shadow-sm bg-white dark:bg-gray-700 text-sm font-medium text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-600">
                                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                                    <path d="M6.29 18.251c7.547 0 11.675-6.253 11.675-11.675 0-.178 0-.355-.012-.53A8.348 8.348 0 0020 3.92a8.19 8.19 0 01-2.357.646 4.118 4.118 0 001.804-2.27 8.224 8.224 0 01-2.605.996 4.107 4.107 0 00-6.993 3.743 11.65 11.65 0 01-8.457-4.287 4.106 4.106 0 001.27 5.477A4.073 4.073 0 01.8 7.713v.052a4.105 4.105 0 003.292 4.022 4.095 4.095 0 01-1.853.07 4.108 4.108 0 003.834 2.85A8.233 8.233 0 010 16.407a11.616 11.616 0 006.29 1.84" />
                                </svg>
                            </a>
                        </div>
                    </div>
                </div>

                <div class="mt-6 text-center">
                    <p class="text-sm text-gray-500 dark:text-gray-400">
                        حساب کاربری ندارید؟
                        <button @onclick="@(()=>SwitchToSmsTab())" class="font-medium text-primary dark:text-primary-dark hover:underline">ثبت نام کنید</button>
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>

@code {
    private LoginAccount? loginAccount = new();
    private SendSmsOtpCode? SendSmsOtpCode = new();
    private VerifySendSmsOtpCode? verifySendSmsOtpCode = new();
    private bool isPasswordTabActive = true;
    private bool CodeSended = false;
    protected override async Task OnInitializedAsync()
    {
        CodeSended = false;
    }
    private async Task LoginBySmsCode()
    {
        if (string.IsNullOrEmpty(SendSmsOtpCode.PhoneNumber))
        {
            var result = await Swal.FireAsync(new SweetAlertOptions
            {
                Title = "پیام",
                Text = "شماره موبایل را وارد کنید",
                Icon = ResultMessageApi.Error,
                ShowConfirmButton = true,

            });
            return;
        }
        var data = await RootApiSendSmsOtpCode.RunMethodApi($"SmsOtpCodes/SendCode/Public", SendSmsOtpCode, method: Method.Post);
        if (data!=null && data.Status == ResultMessageApi.Success)
        {
            await JS.InvokeVoidAsync("startCountdown");
            CodeSended = true;
        }
        else if (data!=null&&  data.Status == ResultMessageApi.ErrorShowBoxCode)
        {
            await JS.InvokeVoidAsync("startCountdown");
            CodeSended = true;
        }
        else if (data!=null)
        {
            var result = await Swal.FireAsync(new SweetAlertOptions
            {
                Title = "پیام",
                Text = data.Message,
                Icon = ResultMessageApi.Error,
                ShowConfirmButton = true,

            });
        }
        else 
        {
            var result = await Swal.FireAsync(new SweetAlertOptions
            {
                Title = "پیام",
                Text = "خطایی رخ داده است",
                Icon = ResultMessageApi.Error,
                ShowConfirmButton = true,

            });
        }

    }
    private async Task VerifySmsCode()
    {
        if (string.IsNullOrEmpty(verifySendSmsOtpCode.Code))
        {
            _= await Swal.FireAsync(new SweetAlertOptions
            {
                Title = "پیام",
                Text = "کد یکبار مصرف ارسال شده را وارد نمایید",
                Icon = ResultMessageApi.Error,
                ShowConfirmButton = true,

            });
            return;
        }
        verifySendSmsOtpCode.PhoneNumber = SendSmsOtpCode.PhoneNumber;
        var data = await RootApiSendSmsOtpCode.RunMethodApi($"SmsOtpCodes/Verify/Public", verifySendSmsOtpCode, method: Method.Post);
        if (data!=null&& data.Status == ResultMessageApi.Success)
        {
            var result = await Swal.FireAsync(new SweetAlertOptions
            {
                Title = "پیام",
                Text = "با موفقیت وارد اکانت خود شده اید",
                Icon = ResultMessageApi.Success,
                ShowConfirmButton = true,

            });
            await ((AccountAuthentication)AuthStateProvider).MarkUserAsAuthenticated(data.Entity);
            // await JS.InvokeVoidAsync("blazorSetCookie", AuthenticationConstant.AccessToken, data.Token, 7);
            NavManager.NavigateTo("/");

        }

        else if (data != null && data.Status == ResultMessageApi.Error)
        {
            var result = await Swal.FireAsync(new SweetAlertOptions
            {
                Title = "پیام",
                Text = data.Message,
                Icon = ResultMessageApi.Error,
                ShowConfirmButton = true,

            });
        }
        else 
        {
            var result = await Swal.FireAsync(new SweetAlertOptions
            {
                Title = "پیام",
                Text = "ارتباط با سرور قطع می باشد",
                Icon = ResultMessageApi.Error,
                ShowConfirmButton = true,

            });
        }
    }
    private async Task LoginAccount()
    {
        loginAccount.SecurityStamp = Guid.NewGuid().ToString();
        var data = await RootApiLogin.RunMethodApi($"auth/Login", loginAccount, method: Method.Post);
        if (data!=null && data.Status == ResultMessageApi.Success)
        {
            await ((AccountAuthentication)AuthStateProvider).MarkUserAsAuthenticated(data.Entity);
            //await JS.InvokeVoidAsync("blazorSetCookie", AuthenticationConstant.AccessToken, data.Token, 7);

            NavManager.NavigateTo("/");
        }
        else if (data!=null )
        {
            var result = await Swal.FireAsync(new SweetAlertOptions
                {
                    Title = "پیام",
                    Text = data.Message,
                    Icon = ResultMessageApi.Error,
                    ShowConfirmButton = true,

                });
        }
        else 
        {
            var result = await Swal.FireAsync(new SweetAlertOptions
                {
                    Title = "پیام",
                    Text = "خطایی رخ داده است",
                    Icon = ResultMessageApi.Error,
                    ShowConfirmButton = true,

                });
        }
    }

    private void SwitchToPasswordTab()
    {
        isPasswordTabActive = true;
    }

    private void SwitchToSmsTab()
    {
        isPasswordTabActive = false;
    }
}